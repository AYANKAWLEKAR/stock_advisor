import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Stock Advisor AI - Intelligent Portfolio Management',
  description: 'AI-powered stock recommendations with risk-based portfolio optimization',
  keywords: 'stock advisor, AI, portfolio, investment, recommendations, risk management',
  authors: [{ name: 'Stock Advisor AI' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100">
          {children}
        </div>
      </body>
    </html>
  )
}
